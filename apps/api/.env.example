# =============================================================================
# DEPRECATED: Use the root .env file instead
# =============================================================================
# All environment variables are now consolidated in the root .env file.
# See /.env.example for the full template.
#
# Usage:
#   cp .env.example .env   (from the repo root)
#   docker compose up
# =============================================================================
